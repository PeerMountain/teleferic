const messageByHash = require('./messageByHash')
const messageByReader = require('./messageByReader')
const messageByDate = require('./messageByDate')
// const messageByDossierHash = require('./messageByDossierHash')
const objectByHash = require('./objectByHash')
const teleferic = require('./teleferic')
const persona = require('./persona')

module.exports = {
    messageByHash,
    messageByReader,
    messageByDate,
    objectByHash,
    teleferic,
    persona
}
